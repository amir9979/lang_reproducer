diff --git a/src/main/java/org/apache/commons/lang3/CharSequenceUtils.java b/src/main/java/org/apache/commons/lang3/CharSequenceUtils.java
index 17669ba00..8681b7f98 100644
--- a/src/main/java/org/apache/commons/lang3/CharSequenceUtils.java
+++ b/src/main/java/org/apache/commons/lang3/CharSequenceUtils.java
@@ -109,7 +109,6 @@ public class CharSequenceUtils {
                     return i;
                 }
             }
-            return NOT_FOUND;
         }
         //supplementary characters (LANG1300)
         if (searchChar <= Character.MAX_CODE_POINT) {
@@ -196,7 +195,6 @@ public class CharSequenceUtils {
                     return i;
                 }
             }
-            return NOT_FOUND;
         }
         //supplementary characters (LANG1300)
         //NOTE - we must do a forward traversal for this to avoid duplicating code points
@@ -246,7 +244,7 @@ public class CharSequenceUtils {
         }
 
         if (start < 0 || len2 < 0 || len2 > len1) {
-            return NOT_FOUND;
+            return -1;
         }
 
         if (len2 == 0) {
@@ -274,7 +272,7 @@ public class CharSequenceUtils {
             while (cs.charAt(i) != char0) {
                 i--;
                 if (i < 0) {
-                    return NOT_FOUND;
+                    return -1;
                 }
             }
             if (checkLaterThan1(cs, searchChar, len2, i)) {
@@ -282,7 +280,7 @@ public class CharSequenceUtils {
             }
             i--;
             if (i < 0) {
-                return NOT_FOUND;
+                return -1;
             }
         }
     }
